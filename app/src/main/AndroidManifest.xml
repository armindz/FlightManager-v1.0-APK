<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".CreateFlightActivity"></activity>
        <activity android:name=".ListFlightsActivity" />
        <activity android:name=".ListAirportsActivity" />
        <activity android:name=".CreateAirportActivity" />
        <activity android:name=".CreateAirlineActivity" />
        <activity android:name=".ListAirlinesActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".CreateActivity" />
        <activity android:name=".HomepageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>